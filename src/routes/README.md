# Telas

Esta pasta agrupa as telas principais da aplicação Vite:

- `index.tsx`: chat financeiro.
- `dashboard.tsx`: indicadores e gráficos.
- `settings.tsx`: ajustes, renda, exportação e limpeza de dados.

As rotas são resolvidas no cliente em `src/App.tsx`.
